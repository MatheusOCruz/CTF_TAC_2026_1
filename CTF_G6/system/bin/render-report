#!/usr/bin/env bash
set -u

record_code="${1:-UNKNOWN}"
template="${2:-clinical}"

printf 'BROOKHAVEN HOSPITAL — INTERNAL REPORT\n'
printf 'Record: %s\n' "$record_code"
printf 'Renderer profile: %s\n' "$template"
printf 'Status: generated by terminal BH-ARCHIVE-03\n'
printf 'Timestamp: %s\n' "$(date '+%Y-%m-%d %H:%M:%S')"
