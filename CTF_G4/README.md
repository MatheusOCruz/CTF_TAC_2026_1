Autor: Augusto Suffert Monteiro

## Como executar o ambiente

```bash
docker compose up -d --build      # constrói e sobe (SSH na porta 2222 do host)
docker compose down               # para e remove
```
